using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
 * This class needs to be able to make the circle follow the mouse each frame
 * around the perimeter of the player, always touching it.
 * */

public class FollowMouseAOE : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
